name = "manish-portfolio-chat"
main = "chat-worker.js"
compatibility_date = "2024-01-01"

# After `wrangler login`, set the secret (not stored in this file or git):
#   wrangler secret put GEMINI_API_KEY
#
# Then deploy:
#   wrangler deploy
